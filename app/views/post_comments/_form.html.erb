<%= form_with(model: [post, post_comment], local: false) do |f| %>
  <%= f.text_area :comment, placeholder: "Comment", class: "w-100 comment-textarea" %>
  <%= f.submit "Comment", class: "btn btn-sm btn-light" %>
<% end %>